
typedef BUGGY = rec X.forall p.( [int,!ref p, exists q.X[q] ] )

// test equals on unfolded stuff.
<t>fun( x : BUGGY[t] ).
	let [a,b,c] = x in
		open <q,y> = c in
         <BUGGY[q],y>
      end
	end
