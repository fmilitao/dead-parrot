
// TODO: delayed type application, feature requirements.

// test of delayed type application (useful on recursive types)

// non-delayed version of type application applies directly to a forall
typedef Test1 = forall p.( (forall q.q)[p])

// the delayed version must carry a pending type application until an unfold
// solves the type variable and then retries to apply.
typedef Test2 = rec X.forall p.( exists q.( X[q] ) )
// note that it is TYPE_VARIABLE [ TYPE ]

"done"
