// WELCOME!

// Dead-Parrot is a prototype implementation of our language written entirely
// in javascript and should run directly in a web-browser.

// Parsing and typechecking errors should be flagged automatically.
// Visit the project page for more details. 

// NOTES:
//  * only one-line comments work.
//  * the languages uses a few IDIOMS for convenient (see idioms example).
//  * examples' files are fetched dynamically (AJAX)

let x = { a = 2, b = "Hello World!", c = false } in
	fun( _ : [] ).(x.b) ({})
end
